/** Короткие подсказки оператору (согласованы с docs/STRUCTURE.md). */

export const DISCOUNT_CATEGORY_NO_DESCENDANTS_HINT =
  'Только выбранные категории — потомки не включаются автоматически. Нужны подкатегории — отметьте их отдельно.';

export const DISCOUNT_OVERLAP_MAX_BENEFIT_HINT =
  'При пересечении кампаний на checkout применится одна с максимальной выгодой (скидки не суммируются).';

/** База вознаграждения — только позиции из области скидки. */
export const DISCOUNT_REWARD_BASE_HINT =
  'PERCENT — % от суммы позиций из области скидки. FIXED — фиксированные ₽ с этих же позиций (не со всей корзины).';

export const DISCOUNT_CONDITIONS_SCOPE_HINT =
  'Пороги (кол-во / сумма / позиции) считаются только по товарам из области скидки, не по всей корзине.';
